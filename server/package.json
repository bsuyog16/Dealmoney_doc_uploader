{
  "name": "localhost-ssl",
  "version": "0.0.1",
  "description": "Serve localhost files in (self-signed) SSL. Also Reload browser tab when the file changes(Run your app in first tab)",
  "dependencies": {
    "express": "*",
    "node-watch": "*",
    "openssl-nodejs": "^1.0.5",
    "underscore": "*"
  },
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "ssl",
    "localhost",
    "express"
  ],
  "author": "Raja Rao DV",
  "license": "MIT",
  "preferGlobal": "true",
  "bin": {
    "localhost-ssl": "./app.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/rajaraodv/localhost-ssl.git"
  }
}
